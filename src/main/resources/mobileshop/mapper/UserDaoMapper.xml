<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mobileshop.dao.UserDao">
    <select id="findByUserName" parameterType="string"
            resultType="mobileshop.entity.UserEntity">
        select * from ms_user where username=#{username}
    </select>
    <select id="findAll" resultType="mobileshop.entity.UserEntity">
        select * from ms_user
    </select>
    <insert id="save" parameterType="mobileshop.entity.UserEntity">
        insert into ms_user(username,password,email) values(#{username},#{password},#{email})
    </insert>
    <select id="existUserNameOrEmail" parameterType="string" resultType="boolean">
        select count(*) from ms_user where username=#{username} or email=#{email}
    </select>
</mapper>
